import { React } from 'react';

const style = {
	position: 'absolute',
	top: 0,
	right: 0,
	height: '100%',
	width: '50%',
	background: 'bisque',
};

const TaskPane = () =>
<div style={style}> Task </div>;

export default TaskPane;
